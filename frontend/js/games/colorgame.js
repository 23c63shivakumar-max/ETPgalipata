// colorgame.js
export function initColorGame(){ const colorCircle = document.getElementById('colorCircle'); const startColorGame = document.getElementById('startColorGame'); if (!colorCircle || !startColorGame) return; let breathingActive=false; startColorGame.addEventListener('click', ()=>{ if (breathingActive) return; breathingActive=true; startColorGame.textContent='Relax...'; let isBlue=true; const breathe=setInterval(()=>{ colorCircle.style.background = isBlue?'purple':'blue'; isBlue=!isBlue; },4000); setTimeout(()=>{ clearInterval(breathe); breathingActive=false; startColorGame.textContent='Start Again'; colorCircle.style.background='blue'; },20000); }); }