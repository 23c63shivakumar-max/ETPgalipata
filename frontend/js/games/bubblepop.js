// bubblepop.js
export function initBubblePop(){ const bubbleContainer = document.getElementById('bubbleContainer'); const bubblePointsDisplay = document.getElementById('bubblePoints'); if (!bubbleContainer) return; let bubblePoints=0; function createBubble(){ const b=document.createElement('div'); b.classList.add('bubble-pop'); b.style.left = Math.random()*260 + 'px'; b.style.animationDuration = (3+Math.random()*3) + 's'; b.onclick = ()=>{ b.remove(); bubblePoints++; bubblePointsDisplay && (bubblePointsDisplay.textContent = bubblePoints); }; bubbleContainer.appendChild(b); setTimeout(()=> b.remove(),6000); } setInterval(createBubble,1000); }